let a = parseInt(prompt('Inserisci il primo numero'));
let b = parseInt(prompt('Inserisci il secondo numero'));

let somma = a + b;
let dif = a - b;
let mol = a * b;
let div = a / b;
let pot = a ** b;

console.log('Con i numeri d ate scelti, i risultati delle varie operazioni sono: ');

console.log(`somma(${somma}), sottrazione(${dif}), moltiplicazione(${mol}), divisione(${div}), potenza(${pot}).`);